apiVersion: v1
data:
  GITHUB_SHA: 953e8e2445b7016d9c743150be99e64c141c3679
  NAMESPACE: pr-102
  OVERLAY: staging
  SERVICE_NAME: ingress
kind: ConfigMap
metadata:
  name: ingress-metadata-2mbhg2dt55
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress
spec:
  rules:
  - http:
      paths:
      - backend:
          serviceName: frontend
          servicePort: 80
        path: /
      - backend:
          serviceName: frontend
          servicePort: 80
        path: /static/
        pathType: Prefix
      - backend:
          serviceName: service-foo
          servicePort: 80
        path: /foo
      - backend:
          serviceName: service-bar
          servicePort: 80
        path: /bar
